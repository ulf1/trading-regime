[project]
name = "findticker"
version = "0.1.0"
description = "Daily market data discovery pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "yfinance",
    "pandas",
    "google-cloud-storage",
]

[dependency-groups]
dev = [
    "pytest",
    "responses",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

